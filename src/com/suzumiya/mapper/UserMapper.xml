<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suzumiya.mapper.UserMapper">
    <resultMap id="allUsers" type="User">
        <id column="id" property="id" />
        <result column="username" property="name" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <association property="role" javaType="Role">
            <id column="role_id" property="id" />
            <result column="name" property="name" />
        </association>
        <association property="school" javaType="School">
            <id column="school_id" property="id" />
            <result column="school_name" property="name" />
        </association>
    </resultMap>
    <select id="selectAll" resultMap="allUsers">
        select user.*,role.*,school.id as school_id,school.name as school_name from user,role,school where user.role_id=role.id and user.school_id=school.id
    </select>
    <select id="selectByUid" resultMap="allUsers">
        select user.*,role.*,school.id as school_id,school.name as school_name from user,role,school where user.role_id=role.id and user.id=#{uid} and user.school_id=school.id
    </select>
    <insert id="insertUser">
        insert into user (username, email, role_id, password) value (#{name}, #{email}, #{role_id}, #{password})
    </insert>
    <delete id="deleteUser">
        delete from user where user.id=#{id}
    </delete>
    <select id="userLogin" resultMap="allUsers">
        select user.* from user where username=#{name} and password=#{password}
    </select>
    <select id="checkUserDuplicate" resultMap="allUsers">
        select * from user where username=#{name} or email=#{email}
    </select>
</mapper>