<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suzumiya.mapper.UserMapper">
    <resultMap id="allUsers" type="User">
        <id column="id" property="id" />
        <result column="username" property="name" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <association property="role" javaType="Role">
            <id column="role_id" property="id" />
            <result column="name" property="name" />
        </association>
    </resultMap>
    <select id="selectAll" resultMap="allUsers">
        select user.*,role.* from user,role where user.role_id=role.id
    </select>
    <select id="selectByUid" resultMap="allUsers">
        select user.*,role.* from user,role where user.role_id=role.id and user.id=#{uid}
    </select>
    <insert id="insertUser">
        insert into user (username, email, role_id, password) value (#{name}, #{email}, #{role_id}, #{password})
    </insert>
</mapper>